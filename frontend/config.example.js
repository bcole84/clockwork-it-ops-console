// Copy of the config scripts/deploy.sh generates and uploads as config.js.
// Not used directly — kept in the repo so the shape of the real file is documented.
window.OPS_CONSOLE_CONFIG = {
  apiBaseUrl: "https://REPLACE-ME.execute-api.us-east-1.amazonaws.com",
  apiKey: "REPLACE-ME",
};
